# InitBox profiles

Profiles define the hardware target, the default module order, and board-specific defaults used by `scripts/initbox-installer.sh`.

For Radxa Cubie A5E, use:

```bash
sudo ./scripts/initbox-installer.sh
```

The installer loads `profiles/radxa-cubie-a5e.conf` by default.
